@model IEnumerable<Category>

<table class="table table-striped">
    <thead>
        <tr>
            <th>Name</th>
            <th>Actions</th>
        </tr>
    </thead>
    <tbody>
        @if (Model != null && Model.Any())
        {
            @foreach (var category in Model)
            {
                        <tr>
                            <td>@category.Name</td>
                            <td>
                                <button type="button" class="btn btn-info btn-sm edit-btn" data-id="@category.CategoryId">Edit</button>
                                <button type="button" class="btn btn-danger btn-sm delete-btn" data-id="@category.CategoryId">Delete</button>
                            </td>
                        </tr>
            }
        }
        else
        {
                <tr>
                    <td colspan="2">No categories found.</td>
                </tr>
        }
    </tbody>
</table>